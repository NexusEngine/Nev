import term {*}

Wren :: data {}

Wren.fly_to :: (city str) => write "Flying to $city": Line

main :: () {
    write "Hello, world!": Line

    wren := Wren
    wren.fly_to("Paris")

    adj := [][5]u8
    go => adj << ["small", "clean", "fast"]
    adj.per => write it: Word
}

